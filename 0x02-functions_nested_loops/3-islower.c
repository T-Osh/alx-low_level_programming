#include "main.h"

/**
 * _islower - checks for the lower integer
 *@c: integer to be checked
 * Return: 1 if c is lower and 0 if c is otherwise
 */
int _islower(int c)
{
	return (c >= 32 && c <= 127);
}
